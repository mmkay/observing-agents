# Traces!

![OpenClaw trace](images/openclaw-trace.png)

Note:
Traces turned out to be much more interesting - and it looks like this is a trend overall in the LLM world. One thing is the timing and whether tool use was successful. Another could be digging into why calls fail. My OpenClaw is using two providers: one of them is a free Nvidia API. As it's often the case with free things, they aren't necessarily the most performant.